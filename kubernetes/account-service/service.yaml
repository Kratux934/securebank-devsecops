apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: securebank
  labels:
    app: account-service
    team: devsecops
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: account-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
